'use strict'

require('./_common')
const assert = require('assert')

it('should returns functions', function() {
  assert(desc)
  assert(task)
  assert(command)
  assert(namespace)
  assert(serie)
  assert(parallel)
  assert(fail)
})
